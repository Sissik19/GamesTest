public class TestParcours {
    //Depart musicClass
/*    public void resolution(){
        Game g = new Game();
        Room currentRoom = g.outside;
        String[] direction = {Direction.NORTH.toString(),Direction.SOUTH.toString(),Direction.EAST.toString(),Direction.WEST.toString(),Direction.UP.toString(),Direction.DOWN.toString()};
        int i=1;
        while(i!=21){
            for(int n = 0; n<direction.length; i++){
                if(currentRoom.exist(direction[n])){
                    if(currentRoom.getDoor(direction[n]).getOpen()=="open"){
                        currentRoom = currentRoom.getExist(direction[n]);
                        i +=1;
                    }
                    else if(currentRoom.)
                }
            }
        }
    }*/
}
